import { Tab, Tabs } from 'nextra-theme-docs';
import Card, { Cards } from '@components/core/Card';
import developingGuides from './developing/_meta.en-US.json';
import orchestratingGuides from './orchestrating/_meta.en-US.json';
import toolsGuides from './tools/_meta.en-US.json';

# Guides

This section of the docs includes end-to-end developer guides for integrating Livepeer in your application, as well as guides for participating in the network as an orchestrator or delegator.

<Tabs items={['🧑‍💻 Developing', '🚏 Orchestrating', '🗳 Tools']}>
  <Tab style={{ paddingTop: 8 }}>
    <Cards
      num={3}
      children={Object.keys(developingGuides).map((key, i) => (
        <Card
          key={i}
          title={developingGuides[key].title}
          description={developingGuides[key].description}
          icon={developingGuides[key].icon}
          href={developingGuides[key].href}
        />
      ))}
    />
  </Tab>
  <Tab>
    <Cards
      num={3}
      children={Object.keys(orchestratingGuides).map((key, i) => (
        <Card
          key={i}
          title={orchestratingGuides[key].title}
          description={orchestratingGuides[key].description}
          icon={orchestratingGuides[key].icon}
          href={orchestratingGuides[key].href}
        />
      ))}
    />
  </Tab>
  <Tab>
    <Cards
      num={3}
      children={Object.keys(toolsGuides).map((key, i) => (
        <Card
          key={i}
          title={toolsGuides[key].title}
          description={toolsGuides[key].description}
          icon={toolsGuides[key].icon}
          href={toolsGuides[key].href}
        />
      ))}
    />
  </Tab>
</Tabs>
